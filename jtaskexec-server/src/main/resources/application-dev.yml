spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
justedlev-service:
  auth:
    client:
        url: http://localhost:8888/jaccount/api
  task-exec:
    register-tasks: true
    restore-tasks: true